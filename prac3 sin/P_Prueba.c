#include <stdio.h>
#define hola 50

typedef int entero;

	int main (){
		int a=10,b;
		char b,c,d;
		string cadena,cadena2; 
		
		printf("El valor de a es: %d",a);
		scanf("%s", cadena);
		cadena= "¡japeepepja ++´`´ç`ç´··$";
		if (strlen(cadena)==3){
			strcpy(cadena,cadena2);
		}else{
			printf("Error");
		}
		for(a=0;a<=8;a++){
			b=10;
			do{
				b--
			}while(b==0);
		}
	}
	
